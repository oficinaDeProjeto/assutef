import { MaskDirectiveDirective } from './mask-directive.directive';

describe('MaskDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MaskDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
